<UserControl x:Class="Owlsure.UI.WpfCounterparty.Views.CounterpartyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cmd="clr-namespace:Owlsure.PresentationUtility.Commands;assembly=Owlsure.PresentationUtility"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border 
                            Background="White" CornerRadius="4" Padding="4">
                            <TextBlock Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=Content}" />
                        </Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Padding" Value="4" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>
    <Grid  Background="{StaticResource skin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ComboBox
            Grid.ColumnSpan="2"
            IsSynchronizedWithCurrentItem="True"
            ItemsSource="{Binding Counterparties}"
            cmd:SelectorSelect.Command="{Binding SelectionChangedCommand}"
            cmd:SelectorSelect.CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=SelectedItem}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Border HorizontalAlignment="Stretch" Padding="2" Margin="2" BorderBrush="Blue" BorderThickness="1" CornerRadius="4">
                        <TextBlock Text="{Binding Name}" />
                    </Border>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Code:" />
        <Label Grid.Row="1" Grid.Column="1" Content="{Binding SelectedCounterparty.Code}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Name:" />
        <Label Grid.Row="2" Grid.Column="1" Content="{Binding SelectedCounterparty.Name}"/>
    </Grid>
</UserControl>
